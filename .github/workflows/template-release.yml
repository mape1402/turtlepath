name: Template Release

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - 'templates/TurtlePath.Template/**'
      - '.github/workflows/template-release.yml'
      - '.template.release'
      - 'TEMPLATE_CHANGELOG.md'
  push:
    branches:
      - main
    paths:
      - 'templates/TurtlePath.Template/**'
      - '.github/workflows/template-release.yml'
      - '.template.release'
      - 'TEMPLATE_CHANGELOG.md'

env:
  CHANGELOG_PATH: TEMPLATE_CHANGELOG.md
  CONFIGURATION: Release
  DOTNET_VERSION: 10.0.x
  NUGET_OUTPUT: ./nupkgs
  NUGET_SOURCE: https://api.nuget.org/v3/index.json
  NUGET_USER: ${{ vars.NUGET_USER }}
  RELEASE_BRANCH_PREFIX: template-releases/
  RELEASE_MARKER_PATH: .template.release
  RELEASE_NOTES_PATH: template_release_notes.txt
  RELEASE_BRANCH_PATTERN: ^template-releases/template-v[0-9]+\.[0-9]+\.[0-9]+$
  RELEASE_TAG_PATTERN: ^template-v[0-9]+\.[0-9]+\.[0-9]+$
  TEMPLATE_PROJECT: templates/TurtlePath.Template/TurtlePath.Template.csproj
  TEMPLATE_SOLUTION: templates/TurtlePath.Template/templates/turtlepath-service/TurtlePath.Template.sln
  TEMPLATE_INFO_PATH: templates/TurtlePath.Template/templates/turtlepath-service/turtlepath.template.json

jobs:
  build-and-release-template:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install .NET SDK
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore generated template solution
        run: dotnet restore "$TEMPLATE_SOLUTION"

      - name: Build generated template solution
        run: dotnet build "$TEMPLATE_SOLUTION" --no-restore --configuration "$CONFIGURATION"

      - name: Test generated template solution
        run: dotnet test "$TEMPLATE_SOLUTION" --no-build --configuration "$CONFIGURATION" --verbosity normal

      - name: Resolve template release marker
        id: release
        if: >
          (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
          github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          set -euo pipefail

          echo "should_release=false" >> "$GITHUB_OUTPUT"

          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            BRANCH="${GITHUB_REF_NAME}"

            if [[ ! "$BRANCH" =~ $RELEASE_BRANCH_PATTERN ]]; then
              echo "Manual template releases must run from a branch matching $RELEASE_BRANCH_PATTERN. Current branch: $BRANCH"
              exit 1
            fi

            TAG="${BRANCH#"$RELEASE_BRANCH_PREFIX"}"
          else
            if ! git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" | grep -qx "$RELEASE_MARKER_PATH"; then
              echo "$RELEASE_MARKER_PATH did not change in this push. Skipping template release."
              exit 0
            fi

            if [[ ! -f "$RELEASE_MARKER_PATH" ]]; then
              echo "$RELEASE_MARKER_PATH was removed. Skipping template release."
              exit 0
            fi

            NON_EMPTY_LINES="$(grep -cve '^[[:space:]]*$' "$RELEASE_MARKER_PATH" || true)"
            if [[ "$NON_EMPTY_LINES" != "1" ]]; then
              echo "$RELEASE_MARKER_PATH must contain exactly one non-empty template release tag, for example template-v1.0.0."
              exit 1
            fi

            TAG="$(grep -ve '^[[:space:]]*$' "$RELEASE_MARKER_PATH" | tr -d '[:space:]')"
          fi

          if [[ ! "$TAG" =~ $RELEASE_TAG_PATTERN ]]; then
            echo "Template release tag must match $RELEASE_TAG_PATTERN. Current value: $TAG"
            exit 1
          fi

          VERSION="${TAG#template-v}"
          TAG_LINE=$(grep -m1 -E "^## \[($TAG|$VERSION)\]" "$CHANGELOG_PATH" || true)

          if [[ -z "$TAG_LINE" ]]; then
            echo "$CHANGELOG_PATH must contain a section for $TAG or $VERSION before releasing."
            exit 1
          fi

          BRANCH="$RELEASE_BRANCH_PREFIX$TAG"

          echo "should_release=true" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"
          echo "tag_line=$TAG_LINE" >> "$GITHUB_OUTPUT"

      - name: Create template release branch
        if: steps.release.outputs.should_release == 'true' && github.event_name == 'push'
        shell: bash
        run: |
          set -euo pipefail

          BRANCH="${{ steps.release.outputs.branch }}"
          if git ls-remote --exit-code --heads origin "$BRANCH" >/dev/null 2>&1; then
            echo "Template release branch $BRANCH already exists. Skipping creation."
            exit 0
          fi

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git branch "$BRANCH" "${{ github.sha }}"
          git push origin "refs/heads/$BRANCH"

      - name: Prepare template release notes
        if: steps.release.outputs.should_release == 'true'
        shell: bash
        run: |
          set -euo pipefail

          TAG_LINE="${{ steps.release.outputs.tag_line }}"

          awk -v tag_line="$TAG_LINE" '
            $0 == tag_line { in_section=1; next }
            in_section && /^## / { exit }
            in_section
          ' "$CHANGELOG_PATH" > "$RELEASE_NOTES_PATH"

          if [[ ! -s "$RELEASE_NOTES_PATH" ]]; then
            echo "$CHANGELOG_PATH section for ${{ steps.release.outputs.tag }} is empty."
            exit 1
          fi

      - name: Stamp template version
        if: steps.release.outputs.should_release == 'true'
        shell: bash
        run: |
          set -euo pipefail

          if [[ ! -f "$TEMPLATE_INFO_PATH" ]]; then
            echo "$TEMPLATE_INFO_PATH was not found."
            exit 1
          fi

          python - <<'PY'
          import json
          import os
          from pathlib import Path

          path = Path(os.environ["TEMPLATE_INFO_PATH"])
          version = "${{ steps.release.outputs.version }}"
          document = json.loads(path.read_text(encoding="utf-8"))
          document["version"] = version
          path.write_text(json.dumps(document, indent=2) + "\n", encoding="utf-8")
          PY

      - name: Pack template NuGet package
        if: steps.release.outputs.should_release == 'true'
        run: >
          dotnet pack "$TEMPLATE_PROJECT"
          --configuration "$CONFIGURATION"
          --output "$NUGET_OUTPUT"
          /p:Version=${{ steps.release.outputs.version }}
          /p:PackageVersion=${{ steps.release.outputs.version }}
          /p:MinVerVersionOverride=${{ steps.release.outputs.version }}

      - name: Create or update template release tag
        if: steps.release.outputs.should_release == 'true'
        shell: bash
        run: |
          set -euo pipefail

          TAG="${{ steps.release.outputs.tag }}"
          HEAD_SHA="$(git rev-parse HEAD)"

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          if git ls-remote --exit-code --tags origin "refs/tags/$TAG" >/dev/null 2>&1; then
            git fetch --force origin "refs/tags/$TAG:refs/tags/$TAG"
            TAG_SHA="$(git rev-list -n 1 "$TAG")"

            if [[ "$TAG_SHA" != "$HEAD_SHA" ]]; then
              git tag --force "$TAG" "$HEAD_SHA"
              git push --force origin "refs/tags/$TAG"
            fi
          else
            git tag --force "$TAG" "$HEAD_SHA"
            git push origin "refs/tags/$TAG"
          fi

      - name: Create GitHub Template Release
        if: steps.release.outputs.should_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release.outputs.tag }}
          name: TurtlePath.Template ${{ steps.release.outputs.version }}
          body_path: ${{ env.RELEASE_NOTES_PATH }}
          draft: false
          prerelease: false
          files: |
            ${{ env.NUGET_OUTPUT }}/*.nupkg
            ${{ env.NUGET_OUTPUT }}/*.snupkg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate NuGet trusted publishing user
        if: steps.release.outputs.should_release == 'true'
        shell: bash
        run: |
          if [[ -z "$NUGET_USER" ]]; then
            echo "NuGet publishing user is not configured."
            exit 1
          fi

      - name: NuGet trusted publishing login
        if: steps.release.outputs.should_release == 'true'
        uses: NuGet/login@v1
        id: nuget_login
        with:
          user: ${{ env.NUGET_USER }}

      - name: Push template to NuGet
        if: steps.release.outputs.should_release == 'true'
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob

          packages=("$NUGET_OUTPUT"/TurtlePath.Template.*.nupkg)
          symbols=("$NUGET_OUTPUT"/TurtlePath.Template.*.snupkg)

          if (( ${#packages[@]} == 0 )); then
            echo "No TurtlePath.Template .nupkg files were found in $NUGET_OUTPUT."
            exit 1
          fi

          for package in "${packages[@]}"; do
            dotnet nuget push "$package" -k ${{ steps.nuget_login.outputs.NUGET_API_KEY }} -s "$NUGET_SOURCE" --skip-duplicate
          done

          for symbol in "${symbols[@]}"; do
            dotnet nuget push "$symbol" -k ${{ steps.nuget_login.outputs.NUGET_API_KEY }} -s "$NUGET_SOURCE" --skip-duplicate
          done
